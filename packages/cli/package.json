{
  "name": "@opencode-janitor/cli",
  "version": "0.1.0",
  "description": "CLI daemon and TUI dashboard for automated code reviews",
  "main": "src/index.ts",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.1.19",
    "@opencode-janitor/shared": "workspace:*",
    "chalk": "^5.4.1",
    "commander": "^13.1.0",
    "zod": "^4.1.8"
  }
}
