{
  "name": "@opencode-janitor/cli",
  "version": "0.1.0",
  "description": "CLI daemon and web dashboard for automated code reviews",
  "main": "src/index.ts",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "sync:agent-renderers": "bun run scripts/sync-agent-renderers.ts",
    "build:frontend": "mkdir -p dist/frontend && cp -r src/daemon/frontend/. dist/frontend/",
    "build:cli": "bun build src/index.ts --outdir dist --target bun --format esm",
    "build": "bun run sync:agent-renderers && bun run build:cli && bun run build:frontend",
    "typecheck": "tsc --noEmit",
    "test": "bun run sync:agent-renderers && bun test",
    "dev": "JANITOR_DEV=1 bun run src/index.ts"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.1.19",
    "@opencode-janitor/shared": "workspace:*",
    "chalk": "^5.4.1",
    "commander": "^13.1.0",
    "zod": "^4.1.8"
  }
}
