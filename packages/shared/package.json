{
  "name": "@opencode-janitor/shared",
  "version": "0.1.0",
  "description": "Shared domain logic — schemas, types, config, git utilities",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "zod": "^4.1.8"
  }
}
