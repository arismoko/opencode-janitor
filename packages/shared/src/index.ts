/**
 * @opencode-janitor/shared
 *
 * Shared domain logic for the janitor ecosystem.
 * Will contain schemas, types, config, and git utilities
 * extracted from the plugin as the CLI needs them.
 */

export {};
